Bastyle Helm chart has been rendered.

Release:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

Useful commands:

  kubectl create secret generic {{ .Values.monitoring.grafana.existingSecret }} \
    --from-literal={{ .Values.monitoring.grafana.existingSecretPasswordKey }}='<password>' \
     --dry-run=client -o yaml | kubectl apply -f -

  helm lint infra/helm/bastyle

  helm template {{ .Release.Name }} infra/helm/bastyle

  kubectl get pods -n {{ .Release.Namespace }}
