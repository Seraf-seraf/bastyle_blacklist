{{- if and .Values.postgresql.enabled (not .Values.postgresql.existingSecret) }}
{{- fail "postgresql.existingSecret is required when postgresql.enabled=true. Create Kubernetes Secret manually or via external secret manager." }}
{{- end }}
