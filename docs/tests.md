# Тесты

Документ описывает существующее тестовое покрытие проекта и сценарии, которые
проверяются в тестах.

## Запуск

| Модуль                       | Команда                                              | Что запускает                                                                                                                           |
|------------------------------|------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| Go-бот                       | `cd services/bot && make test`                       | Unit и функциональные тесты Go-бота без build tag `integration`. Часть функциональных тестов поднимает PostgreSQL через testcontainers. |
| Go-бот, интеграционные тесты | `cd services/bot && go test -tags integration ./...` | Тесты с внешними зависимостями, например PostgreSQL и RabbitMQ через testcontainers.                                                     |
| AI matcher                   | `make -C services/aimatcher test`                    | Pytest-тесты Python/FastAPI сервиса.                                                                                                    |
| Docker Compose PostgreSQL    | `make migrate` после `docker compose -f infra/docker-compose.yaml --project-directory . up -d bastyle-postgresql` | Применяет PostgreSQL migrations к постоянному compose-сервису `bastyle-postgresql`; `make db-status` показывает статус goose migrations, `make db-shell` открывает `psql`, `make db-logs` показывает логи БД. |
| PostgreSQL replication lab   | `make pgrp-up && make pgrp-check && make pgrp-failover && make pgrp-down` | Поднимает учебный стенд manual streaming replication, проверяет async WAL stream, replication lag, WAL archive, репликацию контрольной строки и ручной promote standby. |
| Helm observability           | `helm template bastyle infra/helm/bastyle` | Рендерит Prometheus, Grafana, postgres-exporter, scrape targets для bot/AI matcher/PostgreSQL и dashboard provisioning; postgres-exporter получает DSN из `bastyle-config`, а не из отдельного Secret. |

Интеграционные тесты с PostgreSQL и RabbitMQ используют testcontainers и требуют доступный
Docker. Тесты FFmpeg пропускаются, если бинарный файл `ffmpeg` недоступен.
Тест `test_real_model.py` загружает реальную embedding-модель и зависит от
окружения для запуска Transformers.

## Go-бот

| Файл                                                                              | Тип                  | Проверяемые случаи                                                                                                                                                                                                                                                                                                                             |
|-----------------------------------------------------------------------------------|----------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `services/bot/internal/app/observability/health_test.go`                          | Unit                 | Health server стартует на свободном порту, возвращает успешный статус и учитывает состояние проверяемой зависимости.                                                                                                                                                                                                                            |
| `services/bot/internal/app/bootstrap/messaging_test.go`                           | Unit                 | Startup index subscriber сначала выполняет bootstrap catch-up локальных индексов и не запускает RabbitMQ consumer при ошибке bootstrap.                                                                                                                                                                                                         |
| `services/bot/internal/app/observability/metrics_test.go`                         | Unit                 | Metrics server отдает Prometheus text format через `promhttp` с outbox backlog, build info, PostgreSQL pool stats, длительностью служебных DB-запросов и stale-состоянием индексов.                                                                                                                                                              |
| `services/bot/internal/app/app_test.go`                                           | Unit                 | Application shutdown отменяет контекст фоновых runner-ов, дожидается их завершения и остается идемпотентным.                                                                                                                                                                                                                                  |
| `services/bot/internal/config/config_test.go`                                     | Unit                 | YAML-конфиг загружается с явными значениями и дефолтами; валидируются обязательный Telegram token, PostgreSQL-настройки, HTTP proxy, media limits, FFmpeg-настройки и AI-vector config; устаревшие SQLite/video-like поля отклоняются.                                                                                                         |
| `services/bot/internal/adapters/httpclient/client_test.go`                        | Unit                 | HTTP-клиент применяет proxy URL из конфига и возвращает ошибку на некорректный proxy URL.                                                                                                                                                                                                                                                      |
| `services/bot/internal/adapters/telegram/constructors_test.go`                    | Unit                 | Telegram adapter constructors отклоняют `nil` bot dependency.                                                                                                                                                                                                                                                                                  |
| `services/bot/internal/adapters/telegram/message_test.go`                         | Unit                 | Telegram update преобразуется в доменную модель: animation, animated/static sticker, chat type, reply parent chat и bot command; обычные видео игнорируются.                                                                                                                                                                                   |
| `services/bot/internal/adapters/media/ffmpeg_frame_extractor_test.go`             | Unit                 | FFmpeg extractor ограничивает число кадров, останавливается по timeout, возвращает ошибки для битого кадра и упавшей команды, отклоняет неверные зависимости и некорректный extraction plan.                                                                                                                                                   |
| `services/bot/internal/adapters/media/ffmpeg_frame_extractor_integration_test.go` | Интеграционный       | Реальный FFmpeg извлекает кадры из сгенерированных MP4 animation и WEBM sticker, возвращает ошибку на поврежденном файле, соблюдает timeout и отклоняет слишком большой план до запуска FFmpeg.                                                                                                                                                |
| `services/bot/internal/adapters/database/postgres/pool_test.go`                   | Unit                 | PostgreSQL pool config применяет настройки БД и отклоняет некорректный DSN.                                                                                                                                                                                                                                                                    |
| `services/bot/internal/adapters/database/postgres/pool_integration_test.go`       | Интеграционный       | PostgreSQL pool через testcontainers выполняет transaction commit и rollback без сохранения данных после ошибки.                                                                                                                                                                                                                               |
| `services/bot/internal/adapters/indexcheckpoint/postgres/store_test.go`           | Unit                 | PostgreSQL checkpoint store отклоняет некорректные аргументы и использует monotonic update для Watermill checkpoint `(transaction_id, offset)` без движения назад.                                                                                                                                                                             |
| `services/bot/internal/adapters/outbox/postgres/event_reader_test.go`             | Unit                 | Watermill outbox event reader валидирует checkpoint/limit, читает события строго по `(transaction_id, offset)` из `watermill_outbox_events` и разворачивает Watermill forwarder envelope.                                                                                                                                                     |
| `services/bot/internal/adapters/messaging/rabbitmq/subscriber_test.go`            | Unit                 | RabbitMQ index subscriber config строит queue name по replica id/hostname и отклоняет некорректные URL, routing keys, prefetch и интервалы.                                                                                                                                                                                                    |
| `services/bot/internal/adapters/messaging/rabbitmq/subscriber_integration_test.go` | Интеграционный       | RabbitMQ subscriber создает durable quorum queue, получает сигнал из `bastyle.events`, запускает catch-up и подтверждает сообщение manual ack после успешной синхронизации.                                                                                                                                                                    |
| `services/bot/internal/adapters/matching/exact/matcher_test.go`                   | Unit                 | Exact matcher отклоняет отрицательный buffer, ищет блокировки в рамках chat scope и восстанавливает состояние после перезапуска из хранилища.                                                                                                                                                                                                  |
| `services/bot/internal/adapters/matching/imagehash/index_test.go`                 | Unit                 | Linear imagehash index ищет perceptual hashes только в рамках нужного чата.                                                                                                                                                                                                                                                                    |
| `services/bot/internal/adapters/matching/imagehash/matcher_test.go`               | Unit                 | Imagehash matcher блокирует одинаковую картинку с другим Telegram `file_id`, находит повернутую картинку через варианты perception hash и отклоняет некорректный config.                                                                                                                                                                       |
| `services/bot/internal/adapters/matching/imagehash/index_benchmark_test.go`       | Benchmark            | Измеряет производительность поиска в linear imagehash index.                                                                                                                                                                                                                                                                                   |
| `services/bot/internal/adapters/matching/videolike/index_test.go`                 | Unit                 | Video-like index считает matched frame count и ratio, фильтрует по chat scope, отклоняет один совпавший кадр, ratio ниже порога и несовпадение hash version, дедуплицирует hashes при добавлении и разделяет одинаковые frame signatures разных версий.                                                                                        |
| `services/bot/internal/adapters/matching/videolike/fingerprint_test.go`           | Unit                 | Video-like fingerprint сохраняет hashes кадров с метаданными, требует больше одного совпавшего кадра, принимает совпадение по минимальному ratio и отклоняет mismatch hash version.                                                                                                                                                            |
| `services/bot/internal/adapters/matching/videolike/matcher_test.go`               | Unit/функциональный  | Matcher определяет поддерживаемые animation и video sticker, отклоняет static WebP, TGS, обычные video/document и файлы сверх лимитов; обрабатывает ошибки downloader; проверяет обязательный store, сохранение fingerprint, поиск заблокированного контента, загрузку hashes в index и раннее отклонение по duration limit с реальным FFmpeg. |
| `services/bot/internal/adapters/matching/aivector/matcher_test.go`                | Unit                 | AI-vector matcher возвращает static match, применяет video-like rule, пропускает неподдерживаемый контент и пробрасывает ошибку клиента.                                                                                                                                                                                                       |
| `services/bot/internal/adapters/matching/orchestrator/orchestrator_test.go`       | Unit                 | Block orchestrator отклоняет `nil` database dependency, пустой список matchers и `nil` matcher.                                                                                                                                                                                                                                                |
| `services/bot/internal/app/indexsync/synchronizer_test.go`                        | Unit                 | Index synchronizer догоняет Watermill outbox batch-ами по checkpoint, применяет поддерживаемые события строго по порядку, обновляет checkpoint после успешного batch и помечает индекс stale при ошибке применения.                                                                                                                            |
| `services/bot/internal/app/moderation/service_test.go`                            | Unit/функциональный  | `/ban` от администратора блокирует target media и удаляет target plus command; текстовый reply не блокируется; private chat получает информационный ответ без модерации; constructor отклоняет `nil` dependencies; функциональный сценарий сохраняет ban artifacts и Watermill outbox-события в PostgreSQL, затем catch-up применяет событие к runtime index реплики; новая реплика строит индексы из active bans и повторный catch-up старых событий не ломает exact/imagehash/videolike индексы. |
| `services/bot/internal/app/moderation/e2e_test.go`                                | E2E без Telegram API | Полный сценарий приложения: `/ban` блокирует фото, повторная и похожая отправка удаляются; `/ban` от неадминистратора не сохраняет блокировку и не удаляет фото.                                                                                                                                                                               |

## AI matcher

| Файл                                                                | Тип                                 | Проверяемые случаи                                                                                                                                                                                                                                                                                                                                           |
|---------------------------------------------------------------------|-------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `services/aimatcher/ai_vector_service/tests/test_config.py`         | Unit                                | Парсинг human-readable byte size и duration, загрузка max upload size и PostgreSQL-настроек из конфига.                                                                                                                                                                                                                                                      |
| `services/aimatcher/ai_vector_service/tests/test_db.py`             | Unit                                | Создание psycopg pool с настройками, ping/close lifecycle, закрытие отсутствующего pool без ошибки и rollback transaction при исключении.                                                                                                                                                                                                                    |
| `services/aimatcher/ai_vector_service/tests/test_db_integration.py` | Интеграционный                      | Database pool подключается к PostgreSQL container, выполняет transaction и сохраняет данные после commit.                                                                                                                                                                                                                                                    |
| `services/aimatcher/ai_vector_service/tests/test_storage.py`        | Интеграционный                      | PostgresVectorStore сохраняет active bans, загружает данные после переоткрытия pool, деактивирует ban, фильтрует по chat/model revision/vector dimension, сохраняет index state и отклоняет векторы неверной размерности на уровне store и схемы.                                                                                                            |
| `services/aimatcher/ai_vector_service/tests/test_index.py`          | Unit                                | FAISS HNSW index возвращает пустой результат на пустом индексе, находит добавленные bans, идемпотентно пропускает повторное добавление уже существующих ban/frame refs, фильтрует по chat, сохраняется и загружается с диска, пересобирается при stale state, изменении active bans и checksum mismatch, а также отклоняет поврежденный checksum при прямой загрузке.                                   |
| `services/aimatcher/ai_vector_service/tests/test_api.py`            | Unit/интеграционный уровень FastAPI | `/embed` возвращает vectors в порядке файлов и отклоняет невалидные изображения, превышение количества файлов, размера upload и pixel limit; `/health` отражает доступность БД и stale checkpoint индекса; `/ban` сохраняет vectors и требует vector service; vector index service откатывает или сохраняет active ban state при ошибках обновления индекса, применяет сохраненный ban по UID и повторное событие возвращает `already_applied`; `/search` возвращает hits. |
| `services/aimatcher/ai_vector_service/tests/test_api.py`            | Unit/интеграционный уровень FastAPI | `/metrics` возвращает Prometheus text format, содержит AI DB/index metrics, HTTP request counter по route/status и latency histogram; сам `/metrics` не учитывается в RPS.                                                                                                                                                                      |
| `services/aimatcher/ai_vector_service/tests/test_index_events.py`   | Unit                                | AI-vector index synchronizer читает события после Watermill checkpoint, применяет поддерживаемые `media.ban.created.v1`, двигает checkpoint после успешного batch и помечает `ai_vector` checkpoint stale при ошибке применения; RabbitMQ consumer перед запуском фонового потока выполняет bootstrap catch-up и не стартует после ошибки bootstrap.                                     |
| `services/aimatcher/ai_vector_service/tests/test_real_model.py`     | Интеграционный с моделью            | Реальная Transformers image embedding model дает score выше threshold для одинакового и измененного изображения и ниже threshold для отличающегося изображения.                                                                                                                                                                                              |
